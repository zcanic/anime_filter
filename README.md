# Anime Filter 🌸

Anime Filter 是一个高效、美观的本地番剧筛选工具，专为 Bangumi 番剧爱好者打造。它能帮助你以最直观的方式浏览、筛选和标记 2011-2025 年间的 14,000+ 部番剧，生成专属的观看图谱。

![Preview](docs/images/preview.png) *(Preview placeholder)*

## ✨ 核心特性

*   **⚡️ 极致性能**: 基于 Tauri + Rust 构建，启动瞬间完成，处理 23MB+ 数据零延迟。
*   **🎹 高效交互**: 专为键盘党设计的快捷键系统 + 类原生应用的滑动手势，筛选番剧如行云流水。
*   **🎨 美观界面**: 现代化的 React + TailwindCSS UI，支持自适应网格布局（大/中/小卡片）。
*   **🔍 智能筛选**: 支持按年份、评分、标签、观看状态等多维度快速过滤。
*   **📊 数据可视化**: 自动生成个人观赏图谱，包括时间轴、类型雷达图和年度总结（开发中）。
*   **🔒 本地优先**: 所有数据存储在本地 SQLite，无需联网，隐私安全。

## 🛠 技术栈

*   **Frontend**: React 18, TypeScript, TailwindCSS, Zustand, Framer Motion, TanStack Virtual
*   **Backend**: Tauri 2.0 (Rust), SQLite (rusqlite)
*   **Data**: Bangumi 番剧数据 (CSV)

## 🚀 快速开始

### 环境要求

*   Node.js (v18+)
*   Rust (最新稳定版)
*   包管理器 (pnpm/npm/yarn)

### 安装与运行

1.  **克隆仓库**

    ```bash
    git clone https://github.com/zcanic/anime_filter.git
    cd anime_filter
    ```

2.  **安装依赖**

    ```bash
    # 安装前端依赖
    npm install
    # 或者
    pnpm install
    ```

3.  **准备数据**

    确保 `data/full_data.csv` 存在。如果不存在，请从 release 下载或将你自己的数据放入该位置。
    *(目前开发环境硬编码路径，需自行修改 `src-tauri/src/main.rs` 中的路径)*

4.  **开发模式运行**

    ```bash
    npm run tauri dev
    # 或者
    pnpm tauri dev
    ```

5.  **构建发布版本**

    ```bash
    npm run tauri build
    ```

## ⌨️ 快捷键指南

| 按键 | 功能 |
| :--- | :--- |
| `J` / `←` | 标记为 **已看** (Watched) |
| `K` / `↓` | 标记为 **跳过** (Skipped) |
| `L` / `→` | 标记为 **想看** (Wishlist) |
| `Space` | 查看详情 |
| `Shift` + `A/S/D` | 批量标记当前页 |
| `/` | 聚焦搜索框 |

## 🗺️ 开发路线图

- [x] 基础架构搭建 (Tauri + React)
- [x] CSV 数据解析与 SQLite 存储
- [ ] 核心筛选 UI 实现
- [ ] 虚拟滚动卡片列表
- [ ] 数据可视化图表
- [ ] 数据导出功能

## 📄 License

MIT License © 2025 zcanic

---

*Generated with ❤️ by Claude Code*
